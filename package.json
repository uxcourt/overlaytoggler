{
  "name": "symbol-overlay-toggle",
  "description": "A Sketch plug-in to seek specific style names in library-sourced symbols and toggle the styles.",
  "version": "1.1.0",
  "engines": {
    "sketch": ">=49.0"
  },
  "skpm": {
    "name": "symbol-overlay-toggle",
    "manifest": "src/manifest.json",
    "main": "symbol-overlay-toggle.sketchplugin",
    "assets": [
      "assets/**/*"
    ],
    "sketch-assets-file": "sketch-assets/icons.sketch"
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.5.11"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/uxcourt/overlaytoggler.git"
  }
}
